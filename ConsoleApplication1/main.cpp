/*
 * Программа: Меню для выполнения заданий
 *
 * Автор: Иван Андреевич
 *
 * Описание программы:
 * Программа предоставляет пользователю меню для выбора одного из пяти заданий:
 * 1. Работа с классом Bell (имитация звука колокола).
 * 2. Работа с классом OddEvenSeparator (разделение чисел на четные и нечетные).
 * 3. Работа с классом Table (создание и вывод таблицы).
 * 4. Работа с классом Complex (арифметические операции с комплексными числами).
 * 5. Работа с классом Stock (управление складом коробок).
 *
 * Входные данные:
 * - Пользователь выбирает задание через консоль.
 * - Для каждого задания программа запрашивает необходимые данные (например, количество звуков для Bell, числа для OddEvenSeparator и т.д.).
 *
 * Выходные данные:
 * - Результаты выполнения выбранного задания выводятся в консоль.
 *   Например:
 *   - Для Bell: последовательность звуков "ding" и "dong".
 *   - Для OddEvenSeparator: списки четных и нечетных чисел.
 *   - Для Table: вывод таблицы и среднего арифметического её значений.
 *   - Для Complex: результаты арифметических операций с комплексными числами.
 *   - Для Stock: выдача коробок по заданным условиям.
 *
 * Имена входных файлов:
 * - Входные данные вводятся через консоль, файлы не используются.
 *
 * Имена выходных файлов:
 * - Выходные данные выводятся в консоль, файлы не создаются.
 */
#include <iostream>
#include "Menu.h"

int main() {
    setlocale(0, "");
    
    Menu menu;
    int choice;

    do {
        menu.ShowMenu();
        std::cin >> choice;
        menu.RunTask(choice);
    } while (choice != 0);

    return 0;
}