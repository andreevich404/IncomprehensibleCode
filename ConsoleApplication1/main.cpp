/*
 * Лабораторная работа №7: "С++: STL и алгоритмы"
 *
 * Спецификация:
 * --------------------------
 * Выполнил: Толкачев Иван Андреевич
 * Группа: ФИТ-241
 *
 * Программа демонстрирует:
 * 1. Работу с последовательными и ассоциативными контейнерами STL
 * 2. Использование стандартных алгоритмов (copy_if, unique, sort и др.)
 * 3. Реализацию шаблонных функций для работы с контейнерами
 * 4. Обработку ввода/вывода с защитой от некорректных данных
 * 5. Многофайловую структуру проекта с разделением интерфейсов и реализаций
 *
 * Входные данные:
 * - Числовые последовательности (для заданий 2, 5-8)
 * - Слова и строки (для заданий 3-4)
 * - Параметры вводятся через консоль или задаются в коде
 *
 * Выходные данные:
 * - Результаты обработки контейнеров
 * - Статистика по элементам (количество, позиции)
 * - Отсортированные и модифицированные последовательности
 *
 * Файлы проекта:
 * - PrintFunction.h - функция печати элементов контейнера
 * - AssociativeContainers.h/cpp - работа с set, map, unordered_set
 * - Algorithms.h - шаблонные алгоритмы (Duplicate, Process, Unique)
 * - VectorTasks.h/cpp - обработка вектора по заданию 8
 * - Menu.h/cpp - система меню для выбора заданий
 * - main.cpp - точка входа в программу
 */
#include <iostream>
#include "Menu.h"

int main() {
    setlocale(LC_ALL, "");
    int choice;

    do {
        ShowMenu();
        std::cout << "Введите номер задания: ";
        std::cin >> choice;
        if (choice != 0) {
            ExecuteTask(choice);
        }
    } while (choice != 0);

    return 0;
}